# Content Engine feed (site-scoped key with articles:read, created in the
# engine dashboard under Keys). Without these the build still works — the
# /noticias/ pages simply contain no engine articles.
CE_API_URL=https://your-engine-deployment.vercel.app/v1
CE_API_KEY=ce_xxxxxxxxxxxxxxxx
